import { TestBed } from '@angular/core/testing';
import { Network } from './network.component';

describe('Network', () => {
  it('sample test', () => {
    TestBed.configureTestingModule({ declarations: [Network] });

    expect('Network').toEqual('Network');
  });
});
